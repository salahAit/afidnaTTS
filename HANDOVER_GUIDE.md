# 📘 AfidnaTTS — دليل التسليم التقني (Technical Handover Guide)

مرحباً بك أيها المطور الكريم في مشروع **AfidnaTTS**. تم إعداد هذا الدليل الشامل ليضعك في صورة المشروع، معماريته الحالية، ما تم إنجازه، وما يتبقى لإيصاله إلى مرحلة الإنتاج النهائي كـ **محرك ذكي للكتب الصوتية والمناهج التعليمية**.

---

## 🏗️ 1. المعمارية التقنية (Architecture Overview)

يعتمد المشروع على فصل الواجهة عن المحرك الخلفي، مع بنية **Modular Monolith** في الخلفية لضمان سهولة الصيانة والتطوير.

*   **الواجهة الأمامية (Frontend):** 
    *   SvelteKit 5 (مع Runes).
    *   Shadcn-UI & TailwindCSS للتصميم.
    *   `bun:sqlite` لقاعدة البيانات المدمجة.
*   **المحرك الخلفي (Backend):**
    *   FastAPI (Python) كنقطة ارتكاز (API Gateway).
    *   نموذج **F5-TTS** لتوليد الصوت (عالي الجودة للعربية).
    *   بنية معيارية (Modular) مقسمة إلى مهام، معالجة نصوص، وتوليد.
*   **أدوات مساعدة:**
    *   FFmpeg: للمعالجة الصوتية النهائية (Post-processing).
    *   Aeneas/MFA (مُخطط له): للمزامنة النصية-الصوتية الدقيقة.

---

## 🗺️ 2. خريطة الكود (Codebase Guide)

لتسهيل التنقل في المشروع، إليك الهيكلة الأساسية:

### 🐍 المحرك الخلفي (Python Backend) `الجديد V2`
يقع في مجلد `backend/`:
*   `api/`: مسارات (Routes) الـ FastAPI (مثل `/generate` و `/status`).
*   `core/`: الإعدادات المركزية (`config.py`) ونظام السجلات (`logging.py`).
*   `processing/`: 
    *   `normalization/`: معالجة النصوص العربية (الأرقام، الرموز، الاختصارات).
    *   `chunking/`: التقطيع الذكي للنصوص الطويلة بناءً على الفقرات لتجنب انهيار الذاكرة.
*   `tts/`: المحركات (حالياً `f5/engine.py`، و `generation.py` كمنسق عام).
*   `alignment/`: محرك استخراج الطوابع الزمنية (Timestamps) لتفعيل الـ Karaoke.
*   `tasks/`: مدير المهام (Queue Manager) لتتبع تقدم التوليد.
*   `storage/`: (تم إنشاؤه برمجياً) يحتوي على `audio/`، `cache/`، `timestamps/`.

### 🌐 الواجهة الأمامية (SvelteKit Frontend)
يقع في مجلد `src/`:
*   `routes/project/[id]/`: لوحة تحكم المشروع (التوليد، اختيار الأصوات).
*   `lib/components/`: المكونات القابلة لإعادة الاستخدام (أهمها `SmartPlayer.svelte` الذي يدعم المزامنة).
*   `lib/server/schema.ts`: هيكلية قاعدة بيانات SQLite (جداول المشاريع والتوليدات).

---

## ✅ 3. المراحل المنجزة (Completed Phases)

لقد تم إنجاز البنية التحتية الأساسية والحرجة بنجاح:

1.  **إعادة هيكلة الخلفية (Backend Restructuring):** تحويل الكود إلى بنية Modular Monolith قابلة للتوسع.
2.  **معالجة النصوص (Arabic Text Engine):** بناء التطبيع (Normalization) والتنظيف المبدئي للنصوص العربية.
3.  **التقطيع الذكي (Smart Chunking):** تقسيم النص إلى أجزاء صغيرة (حد أقصى 25 ثانية) لمنع الهلوسة واستقرار الـ GPU.
4.  **التوجيه الذكي (Smart Routing):** تأسيس آلية لاختيار محرك TTS المناسب حسب اللغة.
5.  **المزامنة - الواجهة (Alignment UI):** بناء مكون `SmartPlayer.svelte` الذي يتفاعل مع توقيت الكلمات (Karaoke Style).
6.  **التخزين المؤقت (Cache System):** تطبيق نظام Hashing لمنع إعادة توليد نفس النص وتوفير الموارد.
7.  **تحسين الصوت (Post-Processing):** دمج FFmpeg لتوحيد مستوى الصوت (Normalize -14 LUFS) وإزالة الصمت الزائد تلقائياً.

---

## ⏳ 4. المراحل المتبقية لك (Remaining Phases) - خريطة الطريق

إليك ما تحتاج إلى إكماله للوصول للنسخة النهائية:

### أولاً: قاعدة البيانات (المرحلة 11)
*   **الهدف:** تحديث `src/lib/server/schema.ts`.
*   **المطلوب:** دمج مسارات ملفات المزامنة (`timestamps_url`) وحقول إضافية (`metadata`) في جدول التوليدات، وربما فصل الأصوات المخصصة في جدول مستقل `voices`.

### ثانياً: المزامنة الفعليّة (المرحلة 5)
*   **الهدف:** دمج أداة محاذاة قسرية (Forced Alignment) حقيقية في `backend/alignment/engine.py`.
*   **المطلوب:** حالياً المحرك يقوم بـ "مزامنة وهمية/تقديرية". تحتاج إلى استدعاء مكتبة `aeneas` (بسيطة/سريعة) أو **MFA** (أدق للعربية الفصحى) لتوليد JSON حقيقي يحتوي على التوقيت الدقيق لكل كلمة.

### ثالثاً: الترقية إلى WebSockets (المرحلة 7)
*   **الهدف:** تحسين تجربة المستخدم في الواجهة.
*   **المطلوب:** استبدال آلية الـ Polling (سؤال الخادم كل ثانية عبر `setInterval` في SvelteKit) باتصال WebSocket حي مع FastAPI لنقل نسبة التقدم (Progress) بشكل فوري.

### رابعاً: إدارة ذاكرة الـ GPU (المرحلة 9)
*   **الهدف:** منع انهيار الذاكرة (OOM) في التوليدات الطويلة.
*   **المطلوب:** التأكد من تفريغ الـ VRAM (عبر `torch.cuda.empty_cache()`) بين عمليات التقطيع والتوليد إذا قررت دمج F5-TTS في نفس مساحة الذاكرة (in-process) بدلاً من استدعائه كـ Subprocess.

### خامساً: تجميع المقاطع (Stitching)
*   **الهدف:** دمج الصوت النهائي.
*   **المطلوب:** بما أن نظام التقطيع (Chunking) يقسم النص، يجب استخدام FFmpeg في `backend/tts/generation.py` لدمج الملفات الصوتية الصغيرة في ملف واحد نهائي قبل المعالجة النهائية.

---

## 💻 5. كيفية التشغيل (How to Run)

لتبدأ العمل والتطوير، ستحتاج إلى نافذتي موجه أوامر (Terminal):

**1. تشغيل الخادم الخلفي (Backend):**
```bash
cd /home/msi/afidnaTTS
export PYTHONPATH=. 
# تأكد من تفعيل بيئة Python الافتراضية الخاصة بك والتي تحوي F5-TTS
python backend/main.py
```
*(سيعمل الخادم على المنفذ 8000)*

**2. تشغيل الواجهة (Frontend):**
```bash
cd /home/msi/afidnaTTS
bun dev
```
*(ستعمل الواجهة على المنفذ 5173)*

---

بالتوفيق في إكمال هذا النظام الرائع! 🚀
