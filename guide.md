# دليل مشروع AfidnaTTS 🎙️

مشروع **AfidnaTTS** هو منصة احترافية متكاملة لتحويل النص إلى كلام (Text-to-Speech) تعتمد على أحدث تقنيات استنساخ الصوت (Voice Cloning) عبر نموذج F5-TTS، مع واجهة إدارة مشاريع متطورة.

---

## 🏗️ المعمارية التقنية (Architecture)

يعتمد المشروع على فصل الواجهة عن المحرك الخلفي لضمان الأداء والقابلية للتوسع:

1.  **الواجهة الأمامية (Frontend):**
    *   **Framework:** [SvelteKit 5](https://svelte.dev/) (Runes).
    *   **UI Library:** [Shadcn-UI](https://shadcn-svelte.com/) + TailwindCSS.
    *   **Real-time:** دعم WebSockets لتحديثات التقدم الفورية.

2.  **الخلفية الوسيطة (Backend API):**
    *   **Runtime:** Bun.
    *   **Database:** `bun:sqlite` (SQLite).
    *   **Management:** إدارة الأصوات والمزامنة عبر جداول مخصصة.

3.  **محرك التوليد (Generation Engine):**
    *   **Framework:** [FastAPI](https://fastapi.tiangolo.com/) (Python).
    *   **Model:** F5-TTS (يدعم العربية، الإنجليزية، الصينية، إلخ).
    *   **Pipeline:** نظام التقطيع والدمج (Chunking & Stitching) للمقاطع الطويلة.
    *   **Alignment:** محرك استخراج الطوابع الزمنية (Word-level Timestamps).

---

## ✨ المميزات الرئيسية

*   **إدارة مشاريع احترافية:** إنشاء، تعديل، وحفظ المشاريع مع حفظ تلقائي للمحتوى.
*   **مزامنة Karaoke:** مشغل صوت ذكي يعرض الكلمات متزامنة مع الصوت في الوقت الحقيقي.
*   **توليد نصوص طويلة:** القدرة على تحويل كتب كاملة إلى صوت عبر تقنية التقطيع الذكي.
*   **نظام مهام تفاعلي:** رؤية شريط تقدم حقيقي عبر WebSockets.
*   **استنساخ الصوت (Voice Cloning):** إمكانية رفع أصوات مخصصة (Custom Voices) وإدارتها عبر قاعدة البيانات.
*   **دعم تعدد اللغات:** واجهة كاملة تدعم العربية (RTL)، الإنجليزية، والفرنسية.
*   **تصدير مرن:** تحميل الملفات بصيغ WAV، MP3، أو WebM.

---

## 🛠️ التركيب والتشغيل (Setup)

### 1. تشغيل المحرك الخلفي (Python)
```bash
cd /home/msi/afidnaTTS
export PYTHONPATH=.
python backend/main.py
```
*سيعمل الخادم على `http://localhost:8000` ويدعم WebSockets*

### 2. تشغيل الواجهة الأمامية (SvelteKit)
```bash
cd /home/msi/afidnaTTS
bun dev
```
*ستعمل الواجهة على `http://localhost:5173`*

---

## 🗄️ هيكلة البيانات (Database Schema)

تستخدم قاعدة البيانات ثلاثة جداول رئيسية:

*   **projects:** تخزن عنوان المشروع، الوصف، والمحتوى النصي.
*   **generations:** سجل المقاطع المولدة، مسارات الصوت، ملفات المزامنة (Timestamps)، والبيانات الوصفية.
*   **voices:** تخزن إعدادات وأسماء ومسارات الأصوات المخصصة المرفوعة.

---

## 🚀 نظام التوليد (Generation Flow)

1.  يرسل المستخدم طلباً يحتوي على (النص + ID الصوت).
2.  يقوم الباكاند بتقطيع النص (Chunking) لضمان استقرار الذاكرة.
3.  يُنشئ FastAPI مهمة ويفتح قناة **WebSocket** لإرسال التقدم لحظياً.
4.  يتم توليد المقاطع ثم دمجها (Stitching) ومعالجتها نهائياً (Normalize).
5.  يتم حساب المزامنة وحفظ النتائج في قاعدة البيانات وعرضها في المشغل الذكي.

---

## 📈 خارطة الطريق المستقبلية

*   دمج محرك **MFA** لدقة مزامنة احترافية (Millisecond Accuracy).
*   إضافة محرر صوتي بسيط لقص وتعديل المقاطع.
*   دعم نماذج أخرى مثل **Kokoro** للإنجليزية فائقة السرعة.
*   دعم التزامن مع السحابة (Cloud Sync).

---

© 2026 AfidnaTTS Project
💡 *صُمم ليكون بسيطاً، قوياً، ومستقلاً عن أي نموذج محدد.*
