# 🎙️ AfidnaTTS — الدليل التقني الشامل (Comprehensive Technical Guide)

مرحباً بك في مشروع **AfidnaTTS**. هذا الدليل يجمع بين الرؤية الاستراتيجية للمشروع والتفاصيل التقنية الدقيقة لضمان تشغيل وتطوير المنصة كـ **محرك ذكي ومستقل** لتوليد الصوت بالذكاء الاصطناعي.

---

## 🏗️ 1. المعمارية التقنية الموحدة (Unified Architecture)

يعتمد المشروع على بنية **Modular Monolith** مستقلة تماماً، حيث تم دمج محرك الصوت والبيئة الافتراضية داخل بنية المشروع لضمان الاستقرار:

1.  **الواجهة الأمامية (Frontend):** 
    *   **SvelteKit 5**: مع نظام الـ Runes للأداء العالي.
    *   **Shadcn-UI & TailwindCSS 4**: لتصميم عصري وسريع.
    *   **Bun**: كمحرك تشغيل وإدارة حزم.

2.  **المحرك الخلفي (Backend API):**
    *   **FastAPI (Python 3.12)**: كبوابة برمجية (API Gateway) ومدير للمهام.
    *   **WebSocket**: لتحديث التقدم في الوقت الحقيقي (Real-time tracking).

3.  **محرك الصوت المدمج (Unified F5 Engine):**
    *   **الموقع**: `backend/tts/f5/`.
    *   **النقاء البرمجي**: يحتوي فقط على 13 ملفاً برمجياً هي "الجوهر الصافي" لنظام F5-TTS.
    *   **الاستقلالية**: يمتلك بيئته الافتراضية الخاصة (`venv`) لضمان عدم تضارب الاعتماديات.

---

## 🗺️ 2. خريطة الكود (Project Structure)

للمطورين الراغبين في التنقل أو التعديل:

### 🐍 الخلفية (Backend)
- `backend/api/`: مسارات الـ FastAPI (مثل `/generate` و `/status`).
- `backend/core/`: الإعدادات المركزية (`config.py`) ونظام السجلات.
- `backend/processing/`: أنظمة التطبيع (Normalization) والتقطيع الذكي (Chunking).
- `backend/tts/f5/`: المحرك الموحد (القلب النابض للنظام).
- `backend/storage/`: مخزن الأصوات المولدة، الطوابع الزمنية، والنماذج.

### 🌐 الواجهة (Frontend)
- `src/routes/`: المسارات الأساسية (لوحة التحكم والمشاريع).
- `lib/components/`: المكونات التفاعلية مثل `SmartPlayer.svelte` (مشغل الكاريوكي).
- `lib/server/`: هيكلية قاعدة بيانات SQLite وإدارة البيانات.

---

## ✅ 3. ما تم إنجازه (Milestones)

المشروع الآن في حالة "جاهز للإنتاج" (Production Ready) مع الميزات التالية:
1.  **توحيد المحرك (Engine Unification):** دمج F5-TTS بالكامل داخل المشروع لصفر اعتماديات خارجية.
2.  **التنظيف الجراحي (Surgical Cleanup):** إزالة كل الملفات غير الضرورية (آلاف ملفات الأمثلة والتدريب) ليبقى الكود الصافي فقط.
3.  **التقطيع والدمج الذكي:** معالجة النصوص الطويلة جداً دون استهلاك مفرط للذاكرة.
4.  **نظام المزامنة (Karaoke Sync):** عرض الكلمات متزامنة مع الصوت بدقة عالية.

---

## 🚀 4. كيفية التشغيل (How to Run)

لتبدأ العمل، ستحتاج إلى نافذتي Terminal:

### الخطوة 1: تشغيل المحرك (Backend)
```bash
# تفعيل البيئة الافتراضية الرئيسية
source .venv/bin/activate
# تشغيل خادم FastAPI
python -m backend.main
```

### الخطوة 2: تشغيل الواجهة (Frontend)
```bash
# تشغيل SvelteKit باستخدام Bun
bun run dev
```
*ستجد الواجهة على `http://localhost:5173` والمحرك على `http://localhost:8000`.*

---

## 📈 5. خارطة الطريق (Roadmap)
- **دقة المزامنة**: دمج MFA للمواقف التي تتطلب دقة بالميلي ثانية.
- **دعم اللغات**: تحسين نماذج اللغة الإنجليزية والفرنسية باستخدام محرك Kokoro السريع.
- **إدارة النماذج**: واجهة لتبديل نماذج F5 بسهولة من المتصفح.

---

© 2026 AfidnaTTS Project
💡 *صُمم ليكون بسيطاً، قوياً، ومستقلاً تماماً.*
