# دليل مشروع AfidnaTTS 🎙️

مشروع **AfidnaTTS** هو منصة احترافية متكاملة لتحويل النص إلى كلام (Text-to-Speech) تعتمد على أحدث تقنيات استنساخ الصوت (Voice Cloning) عبر نموذج F5-TTS، مع واجهة إدارة مشاريع متطورة.

---

## 🏗️ المعمارية التقنية (Architecture)

يعتمد المشروع على فصل الواجهة عن المحرك الخلفي لضمان الأداء والقابلية للتوسع:

1.  **الواجهة الأمامية (Frontend):**
    *   **Framework:** [SvelteKit 5](https://svelte.dev/) (باستخدام Runes الجديدة).
    *   **UI Library:** [Shadcn-UI](https://shadcn-svelte.com/) + TailwindCSS.
    *   **State Management:** Svelte Stores (RTL, Theme, i18n).
    *   **Icons:** Lucide Svelte.

2.  **الخلفية الوسيطة (Backend API):**
    *   **Runtime:** Bun.
    *   **Database:** `bun:sqlite` (قاعدة بيانات محلية سريعة جداً).
    *   **Processing:** نظام Polling غير متزامن لتتبع مهام التوليد الطويلة.

3.  **محرك التوليد (Generation Engine):**
    *   **Framework:** [FastAPI](https://fastapi.tiangolo.com/) (Python).
    *   **Model:** F5-TTS (يدعم العربية، الإنجليزية، الصينية، إلخ).
    *   **Process Management:** subprocess.Popen لتتبع التقدم (Progress) في الوقت الحقيقي.
    *   **Format Conversion:** FFmpeg لتحويل الصيغ (MP3, WebM).

---

## ✨ المميزات الرئيسية

*   **إدارة مشاريع احترافية:** إنشاء، تعديل، وحذف المشاريع مع حفظ تلقائي للمحتوى.
*   **نظام مهام ذكي (Task System):** رؤية شريط تقدم حقيقي (Progress Bar) أثناء توليد الصوت.
*   **استنساخ الصوت (Voice Cloning):**
    *   أصوات مدمجة (Built-in).
    *   إمكانية رفع أصوات مخصصة (Custom Voices) عبر رفع مقطع صوتي (5-15 ثانية).
*   **دعم تعدد اللغات:** واجهة كاملة تدعم العربية (RTL)، الإنجليزية، والفرنسية.
*   **تصدير مرن:** تحميل الملفات بصيغ WAV (الأصلية)، MP3، أو WebM.
*   **سجل التوليدات:** حفظ تاريخ جميع المقاطع المولدة داخل كل مشروع للرجوع إليها لاحقاً.

---

## 🛠️ التركيب والتشغيل (Setup)

### 1. تشغيل المحرك الخلفي (Python)
يتطلب بيئة Python 3.10+ مع تثبيت F5-TTS.

```bash
cd /home/msi/f5-tts
source venv/bin/activate
python api_server.py
```
*سيعمل الخادم على `http://localhost:8000`*

### 2. تشغيل الواجهة الأمامية (SvelteKit)
```bash
cd /home/msi/afidnaTTS
bun install
bun dev
```
*ستعمل الواجهة على `http://localhost:5173`*

---

## 🗄️ هيكلة البيانات (Database Schema)

تستخدم قاعدة البيانات جدولين رئيسيين:

*   **projects:** تخزن عنوان المشروع، الوصف، والمحتوى النصي.
*   **generations:** تخزن سجل المقاطع المولدة، مسار الملف الصوتي، والنص المستخدم.

يتم حفظ الملفات الصوتية في مجلد `static/audio/` والملفات المرجعية في `static/voices/`.

---

## 🚀 نظام التوليد (Generation Flow)

1.  يرسل المستخدم طلباً يحتوي على (النص + ID الصوت).
2.  تقوم SvelteKit بالتحقق من مسار الصوت المرجعي وإرسال طلب لـ FastAPI.
3.  يُنشئ FastAPI مهمة (Task) في الخلفية ويبدأ عملية التوليد عبر `infer_cli`.
4.  تقوم الواجهة بـ "Polling" (سؤال الخادم كل ثانية) عن حالة المهمة والتقدم.
5.  عند الانتهاء، يتم تحميل الملف وحفظه في المشروع وعرضه للمستخدم.

---

## 📈 خارطة الطريق المستقبلية

*   دعم نماذج أخرى مثل **Fish Speech** أو **Kokoro**.
*   إضافة محرر صوتي بسيط لقص وتعديل المقاطع.
*   إضافة ميزة "التوليد الجماعي" (Batch Processing) لملفات كبيرة.
*   دعم التزامن مع السحابة (Cloud Sync).

---

© 2026 AfidnaTTS Project
💡 *صُمم ليكون بسيطاً، قوياً، ومستقلاً عن أي نموذج محدد.*
