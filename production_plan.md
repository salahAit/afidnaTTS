# AfidnaTTS — خطة التحسين الهندسية الشاملة (Production Upgrade Plan)

## 🎯 الهدف الرئيسي
تحويل AfidnaTTS من منصة تجريبية إلى **محرك ذكي للكتب الصوتية والمناهج التعليمية**، مع التركيز على جودة اللغة العربية، استقرار التوليد، والمزامنة الدقيقة بين النص والصوت.

---

## 🏗️ المرحلة 1: إعادة تنظيم الهيكلة (Backend Modular Monolith)
تحويل المشروع إلى بنية منظمة تسمح بتبديل النماذج بسهولة.

**الهيكل المقترح:**
```text
backend/
├── api/             # FastAPI routes
├── core/            # Config, Constants, Logging
├── tts/             # Engines (F5, Kokoro, Fish)
├── processing/      # Text Normalization, Diacritics
├── alignment/       # Aeneas/MFA for sync
├── tasks/           # Task Queue & Progress
├── database/        # SQLite Models
└── storage/         # Audio, Cache, Voices
```

---

## ✍️ المرحلة 2: محرك معالجة النصوص العربي (Arabic Text Engine)
رفع جودة التوليد عبر معالجة النص قبل وصوله للنموذج.

*   **2.1 التطبيع (Normalization):** تحويل الأرقام (1445هـ)، الرموز (ﷺ)، والاختصارات (د.) إلى كلمات منطوقة.
*   **2.2 التنظيف (Cleaning):** معالجة المسافات، الرموز غير المدعومة، وتكرار الأحرف.
*   **2.3 التشكيل الذكي (Smart Diacritization):**
    *   دمج **CAMeL Tools**.
    *   3 مستويات للتشكيل: **خفيف** (للمقالات)، **متوسط** (للمناهج)، **كامل** (للنصوص الدينية).

---

## ✂️ المرحلة 3: التقطيع الذكي (Smart Chunking Engine)
تقسيم النصوص الطويلة لضمان استقرار النموذج ومنع انهيار الذاكرة (VRAM).

*   **القاعدة:** التقطيع بناءً على الفقرات وعلامات الوقف، وليس عدد الأحرف.
*   **الحدود:**
    *   `MIN_CHUNK_SECONDS = 5`
    *   `MAX_CHUNK_SECONDS = 25` (تم تقليله من 35 لضمان عدم هلوسة F5-TTS).

---

## 🚦 المرحلة 4: التوجيه الذكي (Smart Routing)
اختيار النموذج الأنسب بناءً على اللغة لضمان السرعة والجودة.

```python
ROUTING = {
    "ar": "f5tts",    # جودة عالية للعربية
    "en": "kokoro",   # سرعة فائقة وجودة استوديو
    "fr": "f5tts",    # دعم متعدد اللغات
    "zh": "f5tts"
}
```

---

## 🔗 المرحلة 5: محرك المزامنة (Alignment Engine) 🌟
استخراج توقيت كل كلمة بدقة (Karaoke Style).

*   **الخيار الأول:** **Aeneas** (يعمل على CPU، سريع جداً، ومطابق للنص الأصلي 100%).
*   **الخيار الثاني (Fallback):** **MFA (Montreal Forced Aligner)** في حال الحاجة لدقة أعلى للعربية الفصحى.
*   **الناتج:** ملف JSON يحتوي على `word`, `start`, `end`.

---

## 📱 المرحلة 6: المشغل الذكي (Smart UI Player)
بناء مكون SvelteKit متطور للقراءة التفاعلية.

*   **تظليل الكلمات (Karaoke):** تظليل الكلمة أثناء نطقها.
*   **التمرير التلقائي (Auto Scroll):** التحرك مع القارئ.
*   **التحكم في السرعة:** Playback Speed Control.
*   **تصدير الترجمة:** دعم تصدير ملفات SRT/VTT.

---

## ⚡ المرحلة 7: الاستقرار والأداء
*   **التخزين المؤقت (Caching):** عدم إعادة توليد نفس النص مرتين (عبر Hash للنص والإعدادات).
*   **تحسين الذاكرة (GPU Optimization):** تفريغ الذاكرة `torch.cuda.empty_cache()` بعد كل دورة توليد.
*   **تحسين الصوت (Post-Processing):** توحيد مستوى الصوت (Normalization) وإزالة الصمت الزائد عبر FFmpeg.
*   **تحديث الاتصال:** الانتقال من Polling إلى **WebSockets** (أولوية ثانوية بعد استقرار النظام).

---

## 🚫 أشياء يمنع تضييع الوقت بها (Anti-Goals)
*   ❌ Kubernetes أو الأنظمة الموزعة المعقدة.
*   ❌ Redis أو Kafka (استخدام الطوابير المحلية كافٍ).
*   ❌ البنية السحابية المعقدة (المشروع محلي أولاً).

---

## 🎯 أولويات التنفيذ (Execution Priority)
1.  **Refactoring:** إعادة هيكلة المجلدات.
2.  **Arabic Engine:** التشكيل والتطبيع.
3.  **Chunking:** التقطيع المستقر.
4.  **GPU Opt:** استقرار الذاكرة.
5.  **Alignment:** نظام المزامنة والـ Player.
6.  **Caching:** نظام التخزين المؤقت.

---
© 2026 AfidnaTTS Production Plan
💡 *الجودة قبل التعقيد، والاستقرار قبل الميزات.*
